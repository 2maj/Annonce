{% extends 'default/layout.html.twig' %}

{% set title='Home' %}
{% set type_compte='Admin' %}
{% set namepage="Homee" %}
{% block div %}
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">Formulaire de création de compte</div>
                <div class="panel-body">
                    <div class="col-md-6">
                        <di role="form">
                            {{ form_start(formulaire) }}

                            <label>{{ form_label(formulaire.id) }}</label>
                            <div class="form-group">{{ form_widget(formulaire.id) }}</div>
                            <label>{{ form_label(formulaire.nom) }}</label>
                            <div class="form-group"> {{ form_widget(formulaire.nom) }}</div>

                            {#{ form_row(formulaire.submit) }#}
                            {{ form_end(formulaire) }}
                        </di>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}